# 运行环境
NODE_ENV=production

# 服务端口
PORT=3000

# JWT签名密钥（生产环境必须设置，可使用 openssl rand -base64 32 生成）
JWT_SECRET=your-secret-key-here

# 日志级别 (trace, debug, info, warn, error, fatal)
LOG_LEVEL=info

# 配置文件目录
RESOURCES_DIR=./resources
